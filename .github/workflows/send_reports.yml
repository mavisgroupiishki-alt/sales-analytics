name: Retired Manager Notifications

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo "Automatic manager notifications are disabled for Jarvis v1."
          echo "They require separate recipient and content approval."
          exit 1
