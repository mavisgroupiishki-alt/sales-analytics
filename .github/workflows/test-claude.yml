name: Retired JSON Call Pipeline

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo "This JSON pipeline is retired."
          echo "Jarvis live analysis runs privately on DigitalOcean and stores results in Supabase."
          exit 1
